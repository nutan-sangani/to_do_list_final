<%- include("header") -%>

  <div class="box" id="heading">
    <h1> <%= listTitle %> </h1>
  </div>

  <div class="box">
    <!-- <% for (let i=0; i<newListItems.length; i++) { %>
      <div class="item">
        <input type="checkbox">
        <p><%=  newListItems[i]  %></p>
      </div>
      <% } %> -->
    <%  newListItems.forEach(function(item){  %>
      <form action="/delete"  method="post"  >
        <div class="item"> 
          <input type="checkbox" value=<%= item._id %> name='check' onChange="this.form.submit()"> <!--will act like a submit button-->
          <p><%=  item.name.toUpperCase()  %></p>
          <input type="hidden" name="lname" value=<%=listTitle %> > </input>
        </div>
      </form>
   <%   }); %>

   <!-- to implement delete in custom list, we need to pass the listtitle
   when we post, but how, we dont have any other input. 
   So we use the input of type hidden. -->

      <form class="item" action="/" method="post">
        <input type="text" name="newItem" placeholder="New Item" autocomplete="off">
        <button type="submit" name="list" value=<%= listTitle %> >+</button>
      </form>
  </div>

<%- include("footer") -%>
